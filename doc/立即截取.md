# “立即截取”前端交互逻辑说明

本文档说明在前端点击侧边栏中的“立即截取”按钮（`#capture-btn`）之后，从 UI 到 API 调用、再到结果展示的完整流程。对应实现主要集中在 `fronted/index.html` 中的 `WaveformCapture` 类。

---

## 1. 相关 UI 元素与初始化

- 截取控制面板核心元素：
  - 截取按钮：`#capture-btn`（文案为“📸 立即截取”）。
  - 窗口大小滑块：`#capture-window-slider`，范围 `50 ~ 200`，步进 `10`。
  - 窗口大小显示：`#capture-window-size`，例如“100 帧”。
  - 截取状态指示灯：`#capture-status-indicator`，类名 `indicator {connected|disconnected|connecting}`。
  - 截取状态文本：`#capture-status-text`，如“等待截取 / 截取中... / 截取成功 / 截取失败”。
  - 数据源选择（默认 ROI）：
    - 主信号数据：`#data-source-main`（radio，value=`main`）。
    - ROI 灰度数据：`#data-source-roi`（radio，value=`roi`，默认选中）。
  - 截取信息面板：`#capture-info`，包含：
    - 数据类型：`#captured-data-type`（默认显示“ROI 灰度数据”）。
    - 截取帧数：`#captured-frame-count`。
    - 帧范围：`#captured-frame-range`。
    - 持续时间：`#captured-duration`。
  - 子波形展示区：
    - 容器：`#subwaveform-container`。
    - 统计信息文本：`#subwaveform-info`。
    - 清除按钮：`#clear-subwaveform-btn`。

- 初始化过程：
  - 页面底部脚本中创建实例：
    - `const waveformCapture = new WaveformCapture();`
  - `WaveformCapture` 构造函数在初始化时：
    - 设置默认配置 `captureSettings`：
      - `windowSize = 100`，`minSize = 50`，`maxSize = 200`。
      - `dataSource = 'roi'`，即默认截取 ROI 灰度数据。
    - 在 `initElements()` 中绑定上述 DOM 元素，并将 `#data-source-roi` 设为选中，`#captured-data-type` 初始化为“ROI 灰度数据”。
    - 调用 `initEventListeners()` 挂载事件监听（滑块、radio、按钮等）。
    - 调用 `initSubWaveformChart()` 初始化子波形图对象。
    - 调用 `syncInitialDisplay()` 将滑块与窗口大小显示同步（显示“100 帧”）。

---

## 2. 事件绑定概览

`WaveformCapture.initEventListeners()` 中绑定了与“立即截取”相关的几个关键事件：

- 窗口大小滑块 `#capture-window-slider`：
  - `input` 事件：
    - 将滑块数值转换为整数 `newValue`。
    - 更新 `this.captureSettings.windowSize = newValue`。
    - 更新右侧文案 `#capture-window-size` 为 “{newValue} 帧”。

- 数据源单选框：
  - `#data-source-main` `change`：
    - 若被选中，设置 `this.captureSettings.dataSource = 'main'`。
    - 更新 `#captured-data-type` 文案为“主信号数据”。
  - `#data-source-roi` `change`：
    - 若被选中，设置 `this.captureSettings.dataSource = 'roi'`。
    - 更新 `#captured-data-type` 文案为“ROI 灰度数据”。

- 截取按钮 `#capture-btn`：
  - `click` 事件：
    - 调用 `this.captureWaveform()`，触发真正的截取逻辑。

- 清除按钮 `#clear-subwaveform-btn`：
  - `click` 事件：
    - 调用 `this.clearCapture()`，清空子波形/隐藏结果。

因此，在默认配置下，“立即截取”按钮会以 **ROI 灰度数据** 作为数据源，触发 `WaveformCapture.captureWaveform()` 这一入口函数。

---

## 3. 点击“立即截取”后的主流程（captureWaveform）

对应方法：`WaveformCapture.prototype.captureWaveform`，是一个 `async` 异步函数。整体流程可分为四个阶段：进入截取态 → 调用后端 API → 处理成功结果 → 失败处理与善后。

在当前实现中，**默认数据源为 ROI 灰度数据**，即点击“立即截取”时，会从“最近 N 帧 ROI 平均灰度值”中截取一个窗口（窗口大小由滑块配置，默认 100 帧）。

### 3.1 进入“截取中”状态

点击按钮后，首先更新 UI 状态并防止重复点击：

1. `this.updateStatus('connecting', '截取中...');`
   - 将 `#capture-status-indicator` 的类名设置为 `indicator connecting`，呈现黄色/动画的“正在处理”状态。
   - 将 `#capture-status-text` 文本设为“截取中...”。
2. `this.elements.captureBtn.disabled = true;`
   - 禁用“立即截取”按钮，整个请求过程中无法重复点击，避免并发调用。

### 3.2 根据数据源选择调用对应 API

接下来根据当前配置决定调用哪一个 API：

1. 读取当前数据源：
   - `const dataSource = this.captureSettings.dataSource;`
   - 可能值为 `'main'` 或 `'roi'`。
2. 根据数据源决定调用：
   - 若 `dataSource === 'roi'`：
     - 调用 `ApiService.captureRoiWindow(this.captureSettings.windowSize)`。
     - 后端接口 URL：`GET {serverUrl}/data/roi-window-capture?count={windowSize}`。
     - 用于截取 ROI 灰度数据窗口。
   - 否则（主信号）：
     - 调用 `ApiService.captureWindow(this.captureSettings.windowSize)`。
     - 后端接口 URL：`GET {serverUrl}/data/window-capture?count={windowSize}`。
     - 用于截取主信号波形窗口。
3. API 返回值约定：
   - 正常返回结构类似：
     - `success: true`。
     - `series: [...]` 时间序列数据。
     - 主信号数据：包含 `frame_range`, `capture_metadata.duration` 等字段。
     - ROI 数据：包含 `roi_frame_range`, `main_frame_range`, `capture_metadata.capture_duration` 等字段。
   - 请求错误或异常时：
     - `success: false` 或抛出异常，由前端统一处理。

### 3.3 成功时的数据处理与展示

当 `response && response.success` 为真时，进入成功流程：

1. 保存数据到实例：
   - `this.capturedData = response;`
2. 更新“截取信息面板”：
   - 调用 `this.displayCapturedData(response);`
   - 主要行为：
     - 将 `#capture-info` 的 `display` 改为 `block`，让信息面板可见。
     - 使用 `setTimeout` 延迟 10ms 添加 `show` 类（`this.elements.infoPanel.classList.add('show')`），触发淡入动画。
     - 设定 `#captured-frame-count` 为 `data.series.length`（截取帧数）。
     - 根据数据源分别处理：
       - ROI 模式：
         - `#captured-frame-range` 显示 `ROI 帧 x-y`（使用 `data.roi_frame_range`）。
         - `#captured-duration` 使用 `data.capture_metadata.capture_duration`（秒）。
         - 统计 `data.series` 中 `gray_value` 的最小值、最大值、平均值，计算范围。
         - 在 `#subwaveform-info` 中显示类似 “N ROI帧 | T 秒 | 灰度范围: min-max” 的摘要。
       - 主信号模式：
         - `#captured-frame-range` 显示 `frame_range[0]-frame_range[1]`。
         - `#captured-duration` 使用 `data.capture_metadata.duration`。
         - 统计 `data.series` 中 `value` 的 min/max/avg，并计算范围。
         - 在 `#subwaveform-info` 中显示 “N 帧 | T 秒 | 范围: min-max”。
     - 额外检查数据变化范围：
       - 若值域过小（例如 <5），通过 `console.warn` 提示可能画面接近“直线”，便于调试。
3. 准备子波形数据并渲染：
   - 初始 `let series = response.series;`
   - 若 `dataSource === 'roi'`：
     - 将 ROI 的 `gray_value` 转换为统一的 `value` 字段：
       - `series = response.series.map(pt => ({ t: pt.t, value: pt.gray_value }));`
   - 调用 `this.showSubWaveform(series);`：
     - 若 `series` 为空，打印警告并直接返回。
     - 否则：
       - 将 `#subwaveform-container` 的 `display` 设为 `flex`，使子波形区域可见。
       - 调用 `this.subWaveformChart.setData(series);` 注入数据。
       - 通过 `setTimeout` 延迟 50ms 后给容器添加 `show` 类，触发平滑出现动画。
4. 更新截取状态为成功：
   - `this.updateStatus('connected', '截取成功');`
   - 状态指示灯变为绿色（`indicator connected`），文本显示“截取成功”。
5. 成功视觉与触觉反馈：
   - 调用 `this.addSuccessFeedback();`：
     - 给按钮添加 `success` 类，触发 CSS 中定义的渐变闪烁动画（`@keyframes capture-success`）。
     - 1 秒后移除该类，动画结束。
     - 若浏览器支持 `navigator.vibrate`，调用 `navigator.vibrate(50)`，提供 50ms 的轻微震动反馈（在移动设备上生效）。
6. 控制台日志：
   - 打印 `ROI` 或 `主信号` 模式下的截取概要信息，便于调试与验证。

### 3.4 失败处理与 finally 善后

若 `response.success` 为假或在调用/处理过程中抛出异常，则进入 `catch` 分支：

- 错误处理：
  - `console.error('波形截取失败:', error);`
  - `this.updateStatus('disconnected', '截取失败');`
    - 状态灯变为红色（`indicator disconnected`），文本“截取失败”。
  - `alert('波形截取失败: ' + error.message);`
    - 弹窗提示用户当前截取失败的原因。

- finally 善后：
  - 无论成功还是失败，最后都会执行：
    - `this.elements.captureBtn.disabled = false;`
  - 即重新启用“立即截取”按钮，用户可以再次尝试截取。

---

## 4. 清除截取结果逻辑（与“立即截取”配套）

虽然不是点击“立即截取”本身触发，但与截取流程紧密相关的是“清除”逻辑：

- `WaveformCapture.clearCapture()` 被 `#clear-subwaveform-btn` 按钮调用：
  - 将 `this.capturedData` 置为 `null`。
  - 移除子波形容器和信息面板的 `show` 类，触发隐藏动画。
  - 300ms 后将 `#subwaveform-container` 和 `#capture-info` 的 `display` 设为 `none`。
  - 调用 `this.updateStatus('disconnected', '等待截取');`，回到初始等待状态。

这部分保证用户可以在一次截取查看完结果后，清空状态并准备下一次截取。

---

## 5. 小结：点击“立即截取”后的整体时序

综合以上分析，点击“立即截取”按钮后，前端的大致时序如下：

1. 禁用按钮、状态指示灯切换为“截取中...”（`connecting`）。
2. 根据当前数据源和窗口大小，调用对应的 HTTP API：
   - 主信号：`GET /data/window-capture?count={windowSize}`。
   - ROI 灰度：`GET /data/roi-window-capture?count={windowSize}`。
3. 收到成功响应后：
   - 保存数据到 `WaveformCapture.capturedData`。
   - 更新并显示“截取信息”面板（帧数、帧范围、持续时间等）。
   - 转换为统一格式后，将时间序列传递给 `SubWaveformChart` 绘制子波形。
   - 状态指示灯切换为“截取成功”（绿色），按钮播放成功动画，并尝试触发震动反馈。
4. 若 API 调用或数据处理失败：
   - 切换状态为“截取失败”，提示弹窗说明原因。
5. 无论成功或失败，都会在最后重新启用“立即截取”按钮，允许用户再次操作。

以上即为当前前端在点击“立即截取”按钮后的完整交互与数据处理逻辑。
